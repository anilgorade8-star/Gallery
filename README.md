# Gallery

A responsive image gallery built with React that fetches and displays images from an external API in a clean grid layout. Users can browse, search, and preview images dynamically. The project demonstrates API integration, state management with hooks, and modern UI design.

## Tech Stack
- React + Vite
- CSS (Vanilla)

